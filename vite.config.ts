import { defineConfig } from 'vite'

export default defineConfig({
  // Base path für GitHub Pages - wird automatisch aus Repository-Namen abgeleitet
  // Für custom domain oder root deployment: base: '/'
  base: './',
})
